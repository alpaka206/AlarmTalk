# P45: lib validation/utility 파일 + EmailPasswordForm i18n 전환 (Batch 1)

**날짜**: 2026-04-25
**BACKLOG 항목**: 자가 생성 — P44 저널에서 플래그한 "하드코딩 한국어 문자열 최종 감사"

## 발견된 문제

전체 앱 감사 결과 40+건의 하드코딩 한국어 문자열 발견. 이번 Batch 1에서는 lib validation/utility 파일 6개 + EmailPasswordForm 컴포넌트를 처리.

## 접근

- `alarmForm.ts`의 TFunction 주입 패턴을 재사용 (P8에서 확립된 패턴)
- validation 함수: `(input, t: TFunction)` 시그니처로 변경
- stateView: DEFAULTS를 i18n 키로 변경 + `resolveStateView(variant, t, overrides?)` 패턴
- familyAlarmLabel: `buildFamilyAlarmLabel(alarm, selfUserId, t)` — i18n 보간 `{{name}}` 패턴 사용

## 변경 파일 (19개)

| 파일 | 변경 내용 |
|------|----------|
| `src/lib/authFormValidation.ts` | TFunction 주입, 3건 → i18n 키 |
| `src/lib/familyAlarmForm.ts` | TFunction 주입, 5건 → i18n 키 |
| `src/lib/familyAlarmLabel.ts` | TFunction 주입, 2건 → i18n 키 (보간) |
| `src/lib/speakerPickerState.ts` | TFunction 주입, 2건 → i18n 키 |
| `src/lib/stateView.ts` | TFunction 주입, 4건 → i18n 키 |
| `src/lib/voiceName.ts` | TFunction 주입, 2건 → i18n 키 |
| `src/components/EmailPasswordForm.tsx` | useTranslation 추가 + 15건 → t() 호출 |
| `src/components/StateView.tsx` | useTranslation 추가 + resolveStateView 시그니처 변경 |
| `src/components/CoupleView.tsx` | buildMemberDisplayName(m, t) |
| `src/components/FamilyMemberRow.tsx` | buildMemberDisplayName(m, t) |
| `app/(tabs)/alarms.tsx` | buildFamilyAlarmLabel(item, userId, t) + userId 스토어 추가 |
| `app/family-alarm/create.tsx` | validateFamilyAlarmForm + buildMemberDisplayName에 t 전달 |
| `app/voice/picker.tsx` | sanitizeLabel(draftLabel, t) |
| `app/voice/[id].tsx` | sanitizeVoiceName(draftName, t) |
| `src/i18n/ko.json` | 22키 추가 (authForm 11, familyAlarmForm 5, voiceName 2, familyAlarmLabel 2, stateView 4, speakerPicker 2) |
| `src/i18n/en.json` | 동일 22키 추가 |
| `test/authFormValidation.test.ts` | mock t + 시그니처 변경 |
| `test/emailPasswordForm.test.ts` | mock t + i18n 키 검증 |
| `test/familyAlarmForm.test.ts` | mock t + i18n 키 검증 |
| `test/speakerPickerState.test.ts` | mock t + i18n 키 검증 |
| `test/voiceName.test.ts` | mock t + i18n 키 검증 |
| `test/familyAlarmLabel.test.ts` | mock t (보간) + 시그니처 변경 |
| `test/stateView.test.ts` | mock t + 시그니처 변경 |

## 검증

- mobile `npx tsc --noEmit` — 0 errors
- backend `npx tsc --noEmit` — 0 errors
- mobile tests — 450/450 passed
- backend tests — 672/672 passed

## 다음 루프

잔여 하드코딩 한국어 감사:
- voucherShare.ts — 6건 (Batch 2)
- ErrorBoundary.tsx — 4건 (Batch 3)
- character.ts — 40+건 대사/스테이지 라벨 (Batch 4)
- friend/[id].tsx — '친구' fallback 1건
