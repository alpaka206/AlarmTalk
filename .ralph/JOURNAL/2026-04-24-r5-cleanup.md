# R5: 정비 + 테스트 (부분)

**날짜**: 2026-04-24
**BACKLOG 항목**: R5 (정비 + 테스트)

## 수행 내용

### alarm/edit.tsx wake_mode UI 동기화
- `WakeMode` 타입 import 추가
- `wakeMode` 상태 추가 + alarm 데이터에서 초기화
- 깨우기 방식 선택 UI (sound_then_voice / voice_only) 추가 — TTS 모드일 때만 표시
- `editMutation` params에 `wake_mode` 추가
- `handleSubmit`에서 `wake_mode` 전달

### 린트 + 타입체크
- React Compiler lint 에러 1건 수정 (note/create.tsx useMemo dependency)
- 전체 typecheck 통과 (backend + mobile 0 errors)

### 테스트
- backend 553/553 통과
- mobile 168/168 통과

## 미수행 항목
- settings/people 데드 코드 정리 — 기존 settings/people 스택 화면은 아직 다른 곳에서 참조될 수 있으므로 보수적으로 유지
- gift/received.tsx — ProfileDropdown에서 code-register로 변경했지만, 다른 참조가 있을 수 있어 보존

## 변경 파일

| 파일 | 변경 |
|------|------|
| `apps/mobile/app/alarm/edit.tsx` | wake_mode 상태+UI+mutation 추가 |
| `apps/mobile/app/note/create.tsx` | useMemo dependency 수정 (lint fix) |

## 검증

- backend `npx tsc --noEmit` — 0 errors
- mobile `npx tsc --noEmit` — 0 errors
- backend tests — 553/553 passed
- mobile tests — 168/168 passed
