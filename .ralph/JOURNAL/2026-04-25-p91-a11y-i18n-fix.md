# P91 — MiniWaveformPlayer 접근성 라벨 i18n 적용 + 코드베이스 품질 감사

## 선택한 항목
BACKLOG 미완료 항목 모두 blocked (사용자 의존/Notion 접근). 섹션 4 규칙에 따라 "앱 접근성 강화" 선택. 전체 감사 후 유일한 구체적 결함 수정.

## 접근

### 문제 분석
코드베이스 전체 접근성 감사 실시:
- MiniWaveformPlayer.tsx의 재생/일시정지 버튼에 하드코딩된 영어 accessibilityLabel (`'Pause'`, `'Play'`)
- 이미 존재하는 i18n 키 (`player.a11yPlay`, `player.a11yPause`)를 사용하지 않음
- 한국어 사용자에게 스크린 리더가 영어로 읽어주는 문제

### 추가 감사 결과 (수정 불필요)
- **TypeScript any 사용**: 전체 소스에서 1건만 발견 (logger.ts Context<any> — Hono 프레임워크 제약, 정당한 사용)
- **하드코딩된 문자열**: dub/translate.tsx와 ProfileDropdown.tsx의 언어 이름 ('한국어', 'English') — 의도적 (네이티브 스크립트로 표시)
- **기타 접근성**: 모든 인터랙티브 요소에 accessibilityRole/Label 적절히 설정됨
- **테스트 파일 중복 발견**: backend src/routes/*.test.ts (6개)와 test/*.test.ts (6개) 중복 존재 → BACKLOG에 정리 항목 추가

## 변경 파일 (1개)
1. `apps/mobile/src/components/MiniWaveformPlayer.tsx` — import react-i18next 추가, useTranslation() 훅 사용, accessibilityLabel을 t('player.a11yPause')/t('player.a11yPlay')로 변경

## 검증
- typecheck: mobile 0 errors, backend 0 errors
- 테스트: backend 920/920, mobile 662/662
