<?xml version="1.0" encoding="utf-8"?>
<!--
  뒤로가기 셰브론 — **iOS 와 같은 도형**이다.

  ⚠ Material 의 `KeyboardArrowLeft` 나 SF 의 `chevron.backward` 를 쓰지 말 것.
  둘은 다른 아이콘 세트라 획 두께·꼭짓점 처리·비율이 달라, 두 앱을 나란히 놓으면
  안드로이드는 각지고 아이폰은 둥글게 보인다(2026-08-10 사용자 지적).
  24x24 좌표계에 같은 폴리라인을 두고 양쪽에서 같은 값으로 그린다 —
  iOS 대응: `Views/Auth/LoginView.swift` 의 `ChevronBackShape`.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M15,5 L8,12 L15,19"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
