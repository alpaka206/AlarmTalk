<?xml version="1.0" encoding="utf-8"?>
<!--
  하단 탭 아이콘 — **iOS 와 같은 도형**이다(2026-08-10 사용자 요청: "아이폰 아이콘으로 둘 다 통일").

  ⚠ Material 의 `Icons.Outlined/Filled.Menu` 로 되돌리지 말 것. 모양이 다르다 —
  Material 메뉴는 각진 사각 막대라 끝 처리가 다르다(Outlined/Filled path 가 완전히 동일하다).
  ⚠ 값은 **iOS 시뮬레이터에 실제로 그려진 SF Symbol 을 재어** 24 좌표계에 옮긴 것이다
  (SF 글리프 파일을 가져다 쓰지 않는다 — Apple 플랫폼 한정 라이선스라 APK 에 넣을 수 없다).
  iOS 대응: `Views/Auxiliary/AuxiliaryScreen.swift` 의 `NativeTab.systemImage`.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M1.2,6.0 L22.8,6.0"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2.2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
    <path
        android:pathData="M1.2,12.0 L22.8,12.0"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2.2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
    <path
        android:pathData="M1.2,18.0 L22.8,18.0"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2.2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
