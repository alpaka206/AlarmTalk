<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 정식 알람/시계 앱: USE_EXACT_ALARM 은 자동 부여·사용자 회수 불가라 정확 알람을
         안정적으로 보장한다(SCHEDULE_EXACT_ALARM 회수 시 비정확 폴백으로 떨어지는 것 방지).

         ⚠ **API 33+ 에서만 그렇다.** 이 앱의 minSdk 는 26 이라 API 31·32 기기에는
         SCHEDULE_EXACT_ALARM 만 적용되고, 그건 사용자가 회수할 수 있다 — 그 기기들은
         비정확 폴백(setAndAllowWhileIdle)을 **실제로 탄다**. "이 권한이 있으니 비정확
         경로는 안 탄다" 고 읽지 말 것. 그 오독으로 실제 판단을 한 번 그르쳤다.
         남는 한계는 AlarmRepository.reschedulePendingAlarms 의 '알려진 한계' 주석 참고. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".AlarmTalkApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlarmTalk"
        android:usesCleartextTraffic="false">

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <activity
            android:name=".ringing.RingingActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="true"
            android:launchMode="singleTask"
            android:resizeableActivity="false"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:taskAffinity="com.alarmtalk.app.ringing"
            android:turnScreenOn="true"
            android:theme="@style/Theme.AlarmTalk.Ringing" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".alarm.RingingService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- FCM 수신(가족 알람 즉시 배달) — data push 로 백그라운드에서도 즉시 pull. -->
        <service
            android:name=".fcm.AlarmTalkMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".alarm.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- 시간대(여행)·시스템 시각/DST 변경 시 알람 재예약을 위해 수신 -->
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="com.alarmtalk.app.action.DEBUG_RESTORE_ALARMS" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
