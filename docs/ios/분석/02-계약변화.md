Analysis complete. Here are the findings.

## 새 엔드포인트 / 바뀐 응답 (파일:줄)

기준: `a079e2f4` (2026-07-21 21:57) → `HEAD`. 403 커밋.

### 라우트 파일이 통째로 사라짐 (7개)
`packages/backend/src/index.ts` 에서 마운트 해제 + 파일 삭제. 옛 iOS 가 호출하면 **404**.

| 삭제된 파일 | 사라진 엔드포인트 |
| --- | --- |
| `routes/library.ts` | `GET /library`, `PATCH /library/:id/favorite`, `DELETE /library/:id` |
| `routes/friend.ts` | `POST /friend`, `GET /friend/list`, `GET /friend/pending`, `PATCH /friend/:id/accept`, `DELETE /friend/:id` |
| `routes/gift.ts` | `POST /gift`, `GET /gift/received`, `GET /gift/sent`, `PATCH /gift/:id/accept`, `PATCH /gift/:id/reject` |
| `routes/stats.ts` | `GET /stats`, `GET /stats/activity` |
| `routes/family-invite.ts` | `POST /family/invites`, `GET /family/invites`, `POST /family/invites/:code/accept`, `POST /family/invites/:code/revoke` |
| `routes/alarm-source.ts` | `POST /alarm/source` (내 알람 오디오 R2 업로드 — dev/prod 실사용 0건) |
| `routes/billing-apple.ts` | `POST /billing/apple/confirm` |

### 개별 삭제된 엔드포인트
- `GET /user/me` — **삭제됨.** `GET /auth/me` 로 통합 (`routes/auth.ts:748`). 옛 iOS 의 사용자 부트스트랩 경로가 통째로 죽는다.
- `GET /alarm/tick` — 삭제 (`routes/alarm-query.ts`). 서버측 발사 판정 자체가 없어졌다 — 발사는 전적으로 기기 로컬.
- `GET /alarm/:id` — 삭제. 단건 조회 없음, 목록만.
- `DELETE /alarm/:id/decline` → **`POST /alarm/:id/decline`** 로 메서드 변경 (`routes/alarm-mutation.ts:887`).
- `GET /tts/presets` (공개, 무인증) — 삭제. `tts_presets` 테이블도 DROP(마이그레이션 87). 문구 출처는 `lib/stock-clips.ts` 하나로.
- `DELETE /tts/messages/:id` — 삭제.
- `POST /auth/apple` — 삭제.
- `POST /billing/redeem` — 삭제. `POST /code/register` 하나로 통합 (`routes/code.ts:23`).
- `POST /family/alarms` — 삭제. `POST /family/alarms/voice` 만 남음 (`routes/family-alarm.ts:103`).
- `GET /voice/:id`, `GET /voice/:id/stats`, `DELETE /voice/_dev/clear-mine` — 삭제.

### 새 엔드포인트
- **`GET /alarm/declined`** (`routes/alarm-query.ts:92`) — 핵심 신규. 응답:
  ```json
  { "alarm_ids": [], "revoked_alarm_ids": [], "has_more": false }
  ```
  `limit`/`offset` 페이지네이션(상한 100). `alarm_ids`= 수신자가 그만받기 → **알람을 지운다**. `revoked_alarm_ids`= 발신자 탈퇴/동의철회 → **목소리만 걷어내고 알람은 남긴다**. 둘의 처리가 정반대라 섞으면 안 된다.
- **`GET /voice/draft-quota`** (`routes/voice-profile.ts:400`) — `{ limit, used, remaining, registration_limit, registration_used, registration_remaining }`. draft 시도 쿼터(3)와 월간 등록 쿼터(1)가 별개다.
- `GET/POST /admin/login`, `POST /admin/logout` — 운영 콘솔 전용, 클라 무관.

### 바뀐 응답
- **`GET /auth/me`** (`routes/auth.ts:748`) — 응답에 **`token` 이 함께 내려온다**(rolling refresh). 앱은 매번 이 토큰으로 갈아 끼워야 한다. `user` 에 `deletion_status`, `allow_family_alarms`, `family_alarm_quiet_windows`, `dynamic_prompt_settings` 포함. 재발급 실패해도 200 유지(`token` 키 없음 → 쓰던 토큰 유지).
- **알람 응답에서 필드 3개 제거** (`routes/alarm-helpers.ts` `normalizeAlarmRow`): `speaker_id`, `raw_audio_url`, `raw_audio_duration_ms`. 요청 검증에서도 `speaker_id`·`raw_audio_url` 제거.
- **`POST /alarm` 신규 요청 필드 `bucket_id`** (`routes/alarm-mutation.ts:218`) — 무료 버킷 회전 알람용.
- **`GET /user/consents/status`** (`routes/user.ts:441`) — 필드 대폭 추가: `needs_consent`(차단 게이트) / `needs_collection`(화면 띄우기) / `required` / `missing` / `collect` / `optional` / `sensitive_missing` / `has_prior_consent` / `policy_version`. `needs_consent` 와 `needs_collection` 은 **의미가 다르다** — 섞어 쓰면 marketing 재수집이 영영 안 일어나거나 앱이 잠긴다.

### 인증/미들웨어 계약 변화 (`packages/backend/src/middleware/auth.ts`)
- **JWT `sub` 이 `google_id` → `users.id` 로 통일.** 하위 경로가 전부 PK 기준.
- **유저 행 자동생성(즉석 INSERT) 제거 → `401 AUTH_USER_NOT_FOUND`.** 예전엔 토큰만 있으면 계정이 되살아났다.
- **`pending_deletion` 계정의 허용 경로에서 `GET /user/me` 가 빠졌다.** 이제 `DELETE /user/me/deletion` 과 `POST /push/unregister` 둘뿐.
- `ipRateLimitRefundMiddleware` 신설 (`index.ts:216`) — 인증 성공 시 IP 버킷 환불.

---

## 클라가 알아야 하는 스키마 변화

마이그레이션 **78~93 이 16개 추가**(엔트리 75→89). 더불어 **33·49 는 목록에서 삭제**돼 id 에 구멍이 났다(`tts_presets` 시드 전용이라 함께 폐기).

1. **`push_tokens.platform` CHECK 에서 `'ios'` 가 제거됐다** (#88, `lib/migrations.ts:1845`). → **iOS 클라이언트는 FCM 토큰을 등록할 수 없다. DB가 거절한다.** 고쳐 쓰려면 마이그레이션이 하나 더 필요하다.
2. **`store_transactions.provider` 가 `CHECK(provider = 'google')`** 로 좁혀졌다 (#88). Apple/PortOne 결제 행은 물리적으로 저장 불가.
3. **`users.apple_id`, `subscriptions.apple_transaction_id`/`apple_original_transaction_id`/`apple_product_id` 전부 DROP** (#82).
4. `users.picture`, `users.last_active_at` DROP (#79·#85). 프로필 사진 개념 자체가 없다.
5. `alarms.speaker_id`, `alarms.raw_audio_url`, `alarms.raw_audio_duration_ms` DROP (#83·#84). `raw_alarm_uploads` 테이블 DROP.
6. `voice_profiles.voice_gender`, `speech_formality`, `avatar_url` DROP (#83).
7. `users.family_alarm_quiet_days`/`_start`/`_end` DROP (#83) — `family_alarm_quiet_windows`(JSON) 단일 출처로.
8. 죽은 테이블 DROP: `notes`, `voice_speakers`, `friendships`, `gifts`, `dub_jobs`, `plan_group_invites`, `tts_presets` (#79·#83·#87).
9. **`alarm_recipient_state` 에 `revoked`(#92)·`sender_user_id`(#93) 추가** — 받은-알람 소유권/목소리 철회 모델의 핵심.
10. `_kst` 편의 뷰 전면 삭제 (#81).
11. 미래 정책버전 동의기록 격리 (#91) — `policy_version > 4` 인 행을 `'0'` 으로.
12. alarms 인덱스 4종 복구 (#80) — 성능 회귀 수정, 계약 영향 없음.

**플랜/가격은 baseline 이전(#52)이라 옛 iOS도 알고 있다**: personal 3,900 / couple 6,900 / family 14,900원, family max_members 5.

---

## 2026-07-21 이후 새로 생긴 **동작 규약** 목록

`CLAUDE.md` + `AGENTS.md` 에 새로 박힌 것 (각 한 줄):

1. **배포가 마이그레이션보다 먼저 돈다** — 새 컬럼 참조 경로는 반드시 fail-closed(500 내고 롤백), "옛 스키마도 견디게" 만들면 영구히 잘못된 행이 남는다.
2. **받은 알람은 받은 뒤부터 받는 사람 것이다** — 발신자가 지워도 수신자 기기는 그대로 유지, 대신 `sender_user_id` 표식을 먼저 남기고 실패하면 삭제도 안 한다.
3. **탈퇴/동의철회 시 목소리만 걷어내고 알람은 남긴다** — 그만받기(=알람 삭제)와 철회(=목소리만 제거)를 `declined`/`revoked` 로 구분.
4. **입력 규칙은 shared 한 곳에서만** — `DisplayNameSchema`·`normalizeDisplayName`·`clampDisplayName`, `DISPLAY_NAME_MAX_LENGTH`(30)/`VOICE_NAME_MAX_LENGTH`(50), 구글이 준 이름도 외부 입력.
5. **자를 때 서러게이트 쌍을 가르지 말 것** — UTF-16 코드 유닛 단위 slice/take 금지.
6. **거부와 다듬기를 구분한다** — 사용자가 친 값은 스키마로 거부, 외부에서 받은 값은 다듬어 쓴다.
7. **말없이 자르지 말 것** — 상한 초과 시 입력창 아래 이유 표시, 상시 카운터는 두지 않는다.
8. **알람 권한 3종은 필수, 단 막는 건 알람 기능뿐** — 목소리/이용권/설정은 권한 없이도 써야 한다.
9. **어떤 경우에도 "울리지 않아요" 라고 쓰지 말 것** — 셋 중 뭐가 빠져도 알람은 울린다, 권한별 사실대로 말한다.
10. **알람 행 스위치는 저장된 `enabled` 에만 묶는다** — 권한 부족으로 꺼진 것처럼 그리면 끌 수가 없어진다.
11. **1회성 오버레이는 확인이 끝난 뒤에만 판단한다** — `consentChecked`/`versionChecked`/`accountStatusChecked` 준비신호 3종, 성공·실패 모두 `true`, `LaunchedEffect` 키에도 넣을 것.
12. **알람 편집기 기본값 = 직전 선택 유지** — 목소리/문구종류/무료버킷은 계정별 last-used, 기록 시점은 알람 저장 성공 시 한 곳뿐, 적용 대상은 새 알람뿐.
13. **모달 껍데기는 `IosAlertDialog` 하나** — 입력 있는 알럿도 `IosAlertField` 슬롯, 버튼 2개 가로/3개+ 세로, 액션 높이 52dp.
14. **디자인 토큰**: `Waker*Shape` 반경 토큰 + `surfaceContainer*` 5종을 비워 두지 말 것(프레임워크 팝업이 읽는다).
15. **prod DB 전체 초기화 금지, 단 안 쓰는 컬럼·인덱스는 DROP 한다** (2026-08-01 확정).
16. **길이 상한은 서버에도 둔다** — 클라의 `take(n)` 은 앱을 거칠 때만 유효.

---

## Apple 결제 경로의 현재 상태

**존재하지 않는다. 껍데기조차 없다.** 3계층 전부 제거됐다:

1. **라우트 삭제** — `packages/backend/src/routes/billing-apple.ts` (284줄, `POST /billing/apple/confirm`) 가 커밋 `9b2bb385` "chore: 미운영 Apple/iOS 로그인·결제 경로 제거" 에서 삭제. `POST /auth/apple` 과 `AppleLoginRequestSchema` 도 함께.
2. **헬퍼 삭제** — `routes/billing-helpers.ts` 의 `APPLE_PRODUCT_TO_PLAN_KEY` / `applePlanKeyFromProductId` 제거. `POST /billing/cancel` 의 "App Store 구독은 앱스토어에서 해지" 분기(`STORE_CANCEL_UNSUPPORTED`)도 제거.
3. **DB 컬럼·제약 삭제** — 마이그레이션 #82 가 `users.apple_id` + `subscriptions.apple_*` 3컬럼 + 인덱스 3개를 DROP, #88 이 `store_transactions.provider` 를 `CHECK(provider = 'google')` 로 고정.

남은 `apple` 문자열은 전부 `lib/migrations.ts` 의 **역사 기록**과 `lib/consent.ts:81` 의 주석("정책 버전 4에서 Apple 로그인·Apple 수탁 고지를 걷어냈다")뿐. 실행 코드는 0.

결제는 **Google Play 인앱결제 단일 경로**로 확정 — `POST /billing/google/confirm` + RTDN(`routes/billing-google-rtdn.ts`). 개인정보처리방침 v4(2026-07-30)에 그렇게 **명시**돼 있어, Apple 결제를 되살리면 법무 문서 개정 + `CURRENT_POLICY_VERSION` 상향 + 전원 재동의가 따라온다.

---

## "옛 iOS 를 고쳐 쓴다면 반드시 손봐야 하는 것" 체크리스트

**차단급 (P0 — 안 고치면 앱이 아예 안 돈다)**

- [ ] **`GET /user/me` → `GET /auth/me` 전환.** 사용자 부트스트랩 경로가 404. 응답 형태도 다르다(`stats` 없음, `deletion_status`/`dynamic_prompt_settings` 추가).
- [ ] **rolling refresh 구현.** `GET /auth/me` 가 내려주는 `token` 을 매번 갈아 끼워야 한다. 안 하면 90일 뒤 조용히 로그아웃되고, 소유자 게이트에 걸려 **알람이 사라지고 울리지도 않는다.**
- [ ] **`push_tokens.platform='ios'` 를 DB 가 거절한다** (#88). 마이그레이션을 새로 하나 추가해 CHECK 를 되돌리지 않으면 FCM/APNs 토큰 등록 자체가 불가 → 가족알람·목소리공유·철회 신호 전부 못 받는다.
- [ ] **Apple 로그인 제거 대응.** `POST /auth/apple` 없음. Google/이메일만 남는다. Sign in with Apple 을 되살리려면 라우트·스키마·`users.apple_id` 컬럼을 전부 다시 만들어야 하고, **App Store 심사 규정상 소셜 로그인이 있으면 Apple 로그인 필수**라 이건 선택이 아니다.
- [ ] **Apple 결제 전면 재구축.** 위 「현재 상태」 3계층 + 법무 문서 개정 + 정책버전 상향까지.
- [ ] **`POST /user/consents` 에 `document_version` 필수.** 안 보내면 400 `DOCUMENT_VERSION_REQUIRED`, 값이 `'4'` 와 다르면 409 `POLICY_VERSION_MISMATCH`. 법무 문서 전문을 앱 번들에 싣고 그 버전을 보내야 한다.
- [ ] **`GET /app/version` 하한이 Android versionCode 21.** `lib/app-version.ts` 는 platform 파라미터를 받아도 **무조건 Android 정책을 돌려준다**. iOS 빌드번호가 21 미만이면 즉시 강제 업데이트 차단 화면. iOS 분기를 서버에 추가해야 한다.

**계약급 (P1 — 화면이 깨지거나 데이터가 틀어진다)**

- [ ] **삭제된 7개 라우트 호출부 전부 제거** — library / friend / gift / stats / family-invite / alarm-source / billing-apple. 대응 기능(즐겨찾기·친구·선물·통계)은 제품에서 사라졌다.
- [ ] **`POST /billing/redeem` → `POST /code/register`** 로 통합. 바우처·가족초대·프로모를 서버가 판별한다.
- [ ] **`POST /family/alarms` → `POST /family/alarms/voice`**.
- [ ] **`DELETE /alarm/:id/decline` → `POST /alarm/:id/decline`** (메서드 변경).
- [ ] **`GET /alarm/tick` · `GET /alarm/:id` 제거** — 서버측 발사 판정 없음. 발사는 전적으로 로컬(iOS면 `UNNotificationRequest`), 서버는 동기화만.
- [ ] **`GET /alarm/declined` 신규 소비 구현** — `alarm_ids`(지운다) vs `revoked_alarm_ids`(목소리만 걷어낸다) 를 정반대로 처리. 페이지네이션은 두 배열 합만큼 offset 전진.
- [ ] **`GET /tts/presets` 제거** — 문구 출처를 `GET /tts/stock-clips` 로 옮겨야 한다.
- [ ] **알람 필드 제거 대응**: `speaker_id`·`raw_audio_url`·`raw_audio_duration_ms` 를 보내면 무시되고, 응답에서도 안 온다. 내 알람 녹음은 **서버에 올리지 않고 폰에만** 둔다.
- [ ] **`bucket_id` 지원** — 무료 버킷 회전 알람. 없으면 대표 클립 단일 재생으로 폴백.
- [ ] **FCM data-only 타입 4종 처리**: `family_alarm` / `voice_share_changed` / **`voice_access_revoked`(신규)** / **`plan_changed`(신규)**. `voice_access_revoked` 를 무시하면 탈퇴자의 복제 목소리를 계속 들고 울린다 — 음성 생체정보 파기 요구 위반.
- [ ] **동의 상태 신규 필드 대응**: `needs_consent`(차단) vs `needs_collection`(화면 표시) 를 구분, `sensitive_missing` 으로 목소리 등록 화면 인라인 동의, `has_prior_consent` 로 최초/재동의 문구 분기, `optional` 목록은 체크 없이 CTA 통과.
- [ ] **`pending_deletion` 상태에서 `GET /user/me` 로 복구화면을 그리던 경로가 죽는다.** 이제 허용 API 는 `DELETE /user/me/deletion` 과 `POST /push/unregister` 뿐 — 복구 화면은 `GET /auth/me`(authMiddleware 밖)로만 채울 수 있다.
- [ ] **`401 AUTH_USER_NOT_FOUND` 신규 처리** — 자동 계정 생성이 없어졌다.
- [ ] **`GET /voice/draft-quota` 소비** — draft 시도(3)와 월간 등록(1)이 별개 쿼터.
- [ ] **표시 이름 규칙을 shared 와 맞춘다** — 30자(목소리 50자), 제어·제로폭·양방향 문자 제거, 줄바꿈→공백, 서러게이트 쌍 안 가르기. 옛 iOS 는 64자·trim 없음이라 서버가 거절한다.

**동작 규약급 (P2 — 코드는 돌지만 사용자가 손해를 본다)**

- [ ] 1회성 오버레이 준비신호 3종 도입(응답 전에 소진 플래그 태우지 않기).
- [ ] 알람 편집기 last-used 기본값(목소리/문구종류/무료버킷), 저장 성공 시에만 기록, 새 알람에만 적용.
- [ ] 권한 게이트 문구를 "울리지 않아요" 에서 권한별 사실로 교체, 알람 스위치는 저장된 `enabled` 에만 묶기.
- [ ] 세션 정리 시 계정별 신호만 리셋(`versionChecked` 는 유지), 자동 401 에서 취향 저장소를 지우지 않기.

**참고 경로 (전부 절대경로)**
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\index.ts` — 라우트 마운트 현황
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\lib\migrations.ts` — 78~93 신규
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\middleware\auth.ts` — sub=users.id 통일, 자동생성 제거
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\lib\jwt.ts` — `DEFAULT_TTL_SECONDS` 7일→90일
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\lib\consent.ts` — `CURRENT_POLICY_VERSION = '4'`
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\lib\app-version.ts` — minSupported 21 / latest 23
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\backend\src\lib\account-deletion.ts` — `RevokedRecipientTarget`, `purgeUserAccount` 반환값
- `C:\Users\gyuwo\Desktop\AlarmTalk\packages\shared\src\schemas\auth.ts` — 표시이름 단일 출처
- `C:\Users\gyuwo\Desktop\AlarmTalk\apps\android-native\app\src\main\java\com\alarmtalk\app\network\` — 현행 클라 계약 레퍼런스 (8개 Api.kt)

**비용 요약**: 옛 iOS 가 모르는 것은 "엔드포인트 몇 개"가 아니다. ① 세션 모델이 통째로 바뀌었고(sub 의미·90일·rolling), ② 받은-알람 소유권과 목소리 철회라는 **새 도메인 개념**이 생겼으며, ③ iOS 자체가 DB CHECK 제약 수준에서 배제됐고, ④ Apple 로그인/결제는 코드·스키마·법무문서 3중으로 제거됐다. ③④ 만으로도 백엔드 마이그레이션 추가와 법무 문서 개정(→ 전원 재동의)이 선행 조건이다.