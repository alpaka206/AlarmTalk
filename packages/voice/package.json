{
  "name": "@alarmtalk/voice",
  "version": "0.1.0",
  "description": "Voice provider adapter layer (VoiceProvider interface + MockVoiceProvider) for voice_alarm monorepo",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "private": true
}
